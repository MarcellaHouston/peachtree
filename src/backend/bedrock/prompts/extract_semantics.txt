You will be given a user's transcription, and for context, the daily tasks of a user. Use all of these to generate a JSON object with exactly two keys: "semantic" and "summary".
"semantic" should be the current semantically rich queryable string to find relevant talking points from a RAG. Those talking points are generated from the user's daily transcripts in a different functionality, and are insights derived from the user's transcript. The semantic output should be within approximately 60 words, though less is okay. The RAG uses cosine similarity. English grammar does not have to be strictly followed.
"summary" should be a second-person rendition of the semantic output for the user to read. Use "you" language, keep it concise, and stay faithful to the semantic content, and condense to be less than approximately 40 words.
Return ONLY a valid JSON object. NO conversational filler (e.g., "Here is the JSON"). NO markdown formatting. Do NOT wrap the response in json or tags. DO NOT wrap the response in markdown (i.e. ```json). The response MUST start with "{" and end with "}". Any strings in the JSON must be wrapped with double quotes.
Do not summarize/include anything mentioned in the context without it being DIRECTLY referenced in the transcription itself, as we only want to summarize the transcription and the context is there for you to understand more of what's going on.
ITERATION INSTRUCTIONS: If you receive a error message and a suggestion to change in the context, YOU MUST try to respect it and implement the fix suggested.
